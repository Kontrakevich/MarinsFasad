let current=null;const $=id=>document.getElementById(id);async function api(url,options={}){const r=await fetch(url,options);if(!r.ok)throw new Error(await r.text());return r.json()}async function loadHealth(){try{const h=await api('/api/health');$('health').textContent=h.runtime==='standalone-v080'?'system online':'runtime mismatch'}catch(_){$('health').textContent='system offline'}}async function loadProjects(){const items=await api('/api/projects');$('project-count').textContent=String(items.length);$('projects').innerHTML='';items.forEach(p=>{const b=document.createElement('button');b.textContent=p.name;b.dataset.id=p.id;b.classList.toggle('active',current?.id===p.id);b.onclick=()=>selectProject(p.id);$('projects').appendChild(b)})}async function selectProject(id){current=await api('/api/projects/'+id);$('empty').hidden=true;$('workspace').hidden=false;$('project-name').textContent=current.name;renderPipeline();renderImage();await loadProjects()}function renderPipeline(){document.querySelectorAll('#pipeline button').forEach(b=>{const status=current?.pipeline?.[b.dataset.stage]||'locked';b.className=status;b.querySelector('b').textContent=status})}function renderImage(){const key=current?.assets?.source_preview?'source_preview':current?.assets?.source_master?'source_master':null;const img=$('preview');if(!key){img.removeAttribute('src');$('meta').textContent='Нет изображения';return}img.classList.remove('original');img.src=`/api/projects/${current.id}/assets/${key}?t=${Date.now()}`;const c=current.master_canvas;$('meta').textContent=c?`${c.width} × ${c.height} · master canvas`:''}function openTab(name){document.querySelectorAll('.tabs button').forEach(b=>b.classList.toggle('active',b.dataset.tab===name));document.querySelectorAll('.panel').forEach(p=>p.classList.toggle('active',p.dataset.panel===name))}$('new-project').onclick=async()=>{const name=prompt('Название проекта');if(!name)return;const f=new FormData();f.append('name',name);current=await api('/api/projects',{method:'POST',body:f});await loadProjects();await selectProject(current.id)};$('upload').onsubmit=async e=>{e.preventDefault();if(!current)return;const file=$('file').files[0];if(!file)return;const f=new FormData();f.append('file',file);current=await api(`/api/projects/${current.id}/source`,{method:'POST',body:f});renderPipeline();renderImage()};$('preview').onclick=()=>{$('preview').classList.toggle('original')};$('fit').onclick=()=>{$('preview').classList.remove('original')};$('actual').onclick=()=>{$('preview').classList.add('original')};document.querySelectorAll('.tabs button').forEach(b=>b.onclick=()=>openTab(b.dataset.tab));$('save-comment').onclick=async()=>{if(!current)return;const value=$('comment').value.trim();if(!value)return;const f=new FormData();f.append('comment',value);current=await api(`/api/projects/${current.id}/comments/${$('stage').value}`,{method:'POST',body:f});$('comment').value='';renderPipeline();openTab('prompt')};$('show-prompt').onclick=async()=>{if(!current)return;const r=await api(`/api/projects/${current.id}/prompt/${$('stage').value}`);$('prompt').textContent=r.prompt;openTab('prompt')};Promise.all([loadHealth(),loadProjects()]).catch(console.error);